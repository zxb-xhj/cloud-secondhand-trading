import { IDomEditor } from '@wangeditor/editor';
import { Standrad, ToggleTarget } from '../types';
export declare function execToggle(editor: IDomEditor, type: ToggleTarget, standrad: Standrad): void;
