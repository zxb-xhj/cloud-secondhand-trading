import { ToggleIcon } from '../icons';
import { t } from '@wangeditor/editor';
import { execToggle } from './helper';
export class ToggleModeButtonMenu {
    constructor(standrad) {
        this.standrad = standrad;
        this.tag = 'button';
        this.title = t('mode.title');
        this.iconSvg = ToggleIcon;
        this.type = 'editor';
    }
    getValue() {
        return false;
    }
    isActive() {
        return false;
    }
    isDisabled() {
        return false;
    }
    exec(editor) {
        execToggle(editor, this.type, this.standrad);
    }
}
//# sourceMappingURL=button-menu.js.map